import React from "react";

const Button = ({text}) => {
    const onClick = (e) => {
    }
  return ( 
  <button onClick={onClick} className="convert-surface">{text}</button>
  )
}

export default Button;
